{
  "nix.enableLanguageServer": true,
  "nix.serverPath": "nixd",
  "nix.serverSettings": {
    "nixd": {
      "nixpkgs": {
        "expr": "let flake = builtins.getFlake (toString ./.); in import flake.inputs.nixpkgs { system = builtins.currentSystem; overlays = [ flake.inputs.nixgl.overlay ]; }"
      },
      "options": {
        "home-manager": {
          "expr": "(builtins.getFlake (toString ./.)).homeConfigurations.\"moonpie\".options"
        }
      }
    }
  }
}